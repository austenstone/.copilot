# Skill-local noise. Root .gitignore already whitelists skills/**,
# so new skill files are tracked by default. Only ignore generated/runtime junk.

# Internal-only skills vendored from a private GitHub org repo.
# This repo is PUBLIC — these reference internal services, endpoints,
# and monorepo CI details. Never `git add -f` these.
chatops/
copilot-usage-stats/
forma/
my-prs-status/
request-jit/
thehub-docs-search/
watch-ci/

# Field/sales skills — live in github/austenstone-notes (private), not here.
actions-dispatch/
actions-wbr/
fabric-cli-core
fabric-cli-powerbi
fabric-dashboards

.DS_Store
**/.DS_Store
node_modules/
**/node_modules/
*.log
**/*.log
__pycache__/
**/__pycache__/
outputs/
**/outputs/
profile/
**/profile/
google-search copy/
*.sqlite
*.sqlite3
*.db
*.db-shm
*.db-wal
*.bak
*.enc

# Real Slack samples — internal business content, local only.
write/VOICE.md
